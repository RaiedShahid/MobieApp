import React from "react";
import { StyleSheet, Text, View, Button, TextInput } from "react-native";

const PostAdd = (props) => {
	return (
		<View>
			<Text> Post Your Add </Text>
		</View>
	);
};

export default PostAdd;

const styles = StyleSheet.create({});
